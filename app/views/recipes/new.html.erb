<%= form_with model: @recipe do |f| %>
　<%= render 'layouts/errors', obj: f.object %>
<% end %>

<div class="container">

 <div class="row">
  <h4>New Recipe</h4>
 </div>

  <%= form_with model: @recipe, local:true do |f| %>

  <div class="row">
    <div class="col-xs-4 text-center">
     <table>

      <tr>
       <th>レシピ画像</th>
       <td><%= f.attachment_field :image %></td>
      </tr>

      <tr>
       <th>レシピ名</th>
       <td><%= f.text_field :name, placeholder: "レシピ名", size: "30", class: "form-control" %></td>
      </tr>

      <tr>
       <th>ジャンル</th>
       <td><%= f.collection_select :genre_id, Genre.all, :id, :name, size: "30", class: "form-control" %></td>
      </tr>

      <tr>
       <th>材料</th>
       <td></td>
      </tr>

   <!--    <= f.text_field :@ingredient, placeholder: "材料名", size: "30", class: "form-control" %>-->
   <!--    <= f.text_field :@recipe_ingredients, placeholder: "分量", size: "30", class: "form-control" %>-->
   <!--    <= f.text_field :@ingredient, placeholder: "単位", size: "10", class: "form-control" %>-->
   <!--    <= f.submit '+', class: 'btn btn-primary' %>-->

   <!--<= @ingredients.each do |ingredient| %>-->
   <!--<tr>-->
   <!--  <td><= @ingredients.name %></td>-->
   <!--  <td><= @ingredients.recipe_ingredients.quantity %></td>-->
   <!--  <td><= @ingredients.unit %></td>-->
   <!--</tr>-->
   <!--< end %>-->


     <tr>
      <th>レシピ</th>
      <td><%= f.text_area :step, placeholder: "レシピを記入してください", size: "30x5", class: "form-control" %></td>
     </tr>

  </table>
  </div><!--col-->
  </div><!--row-->

  <%= f.submit "レシピを登録", class: 'btn btn-success' %>
  <% end %>

</div><!--container-->
